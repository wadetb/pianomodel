[project]
name = "pianomodel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pretty-midi>=0.2.10",
    "ruff",
    "torch",
    "torchaudio>=2.8.0",
]
