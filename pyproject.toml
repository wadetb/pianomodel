[project]
name = "pianomodel"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "librosa>=0.11.0",
    "numpy",
    "pretty-midi>=0.2.10",
    "ruff",
    "torch",
    "tqdm>=4.67.1",
]
